import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `Map`,
  styles: [
    `
      :host {
        display: block;
      }
    `,
  ],
})
export class AppComponent {}
