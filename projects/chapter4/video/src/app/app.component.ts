import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `Video`,
  styles: [
    `
      :host {
        display: block;
      }
    `,
  ],
})
export class AppComponent {}
